diff --git a/crates/omnyssh-core/src/config/mod.rs b/crates/omnyssh-core/src/config/mod.rs
index 0f713bb..1d95a6a 100644
--- a/crates/omnyssh-core/src/config/mod.rs
+++ b/crates/omnyssh-core/src/config/mod.rs
@@ -114,17 +114,7 @@ pub fn load_all_hosts() -> anyhow::Result<Vec<Host>> {
     // 1. Manual hosts (from hosts.toml).
     let manual = load_hosts()?;
 
-    // 2. Hosts from ~/.ssh/config.
-    let mut ssh_hosts: Vec<Host> = Vec::new();
-    if let Some(ssh_path) = platform::ssh_config_path() {
-        if ssh_path.exists() {
-            match ssh_config::load_from_file(&ssh_path) {
-                Ok(h) => ssh_hosts = h,
-                Err(e) => tracing::warn!("SSH config parse error: {}", e),
-            }
-        }
-    }
-
-    // 3. Merge: manual names take priority.
+    // 2. Do not import hosts from ~/.ssh/config.
+    let ssh_hosts: Vec<Host> = Vec::new();
     Ok(merge_hosts(manual, ssh_hosts))
 }
diff --git a/crates/omnyssh-core/src/utils/platform.rs b/crates/omnyssh-core/src/utils/platform.rs
index 8f188e2..c9e7d15 100644
--- a/crates/omnyssh-core/src/utils/platform.rs
+++ b/crates/omnyssh-core/src/utils/platform.rs
@@ -23,9 +23,12 @@ pub fn ssh_config_path() -> Option<PathBuf> {
 ///
 /// - Linux:   `~/.config/omnyssh/`
 /// - macOS:   `~/Library/Application Support/omnyssh/`
 /// - Windows: `%APPDATA%\omnyssh\`
 pub fn app_config_dir() -> Option<PathBuf> {
-    dirs::config_dir().map(|d| d.join("omnyssh"))
+    dirs::home_dir()
+        .map(|h| h.join(".config"))
+        .or_else(dirs::config_dir)
+        .map(|d| d.join("omnyssh"))
 }
 
 /// Returns the path to the main application config file.
